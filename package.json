{
  "name": "metromatrix-realtime",
  "version": "1.0.0",
  "description": "MetroMatrix chat + call realtime backend (Socket.IO). Shares MongoDB + JWT secret with the main MetroMatrix API for cross-backend consistency.",
  "main": "server.js",
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-async-handler": "^1.2.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.5.0",
    "socket.io": "^4.7.5"
  },
  "devDependencies": { "nodemon": "^3.1.0" }
}
